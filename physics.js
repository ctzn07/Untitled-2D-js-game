import{Vec} from './vector.js';

export class Physics{
    constructor(gameObject, drag){
        this.object = gameObject;
        this.velocity = new Vec(0,0);
        this.drag = drag;
    }
    update(deltaTime){
        {
            
        }
    }
}